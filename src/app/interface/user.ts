export interface User {
    id?: bigint,
    username?: string,
    password?: string,
    role?: string,
    email?: string,
    firstName?: string,
    lastName?: string,
    avatar?: string,
    fullName?: string
}
